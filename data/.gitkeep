# data/field_spec.json (S5) was built on 2026-07-25 by tools/build_field_spec.py.
#
# It is DERIVED, never hand-edited: change docs/area_map.toml (judgement) or the
# extraction, then re-run the builder. It needs a manifest from a full `all` run
# because it pins the whole extraction chain -- see ADR-016.
#
#     uv run python tools/build_field_spec.py
#
# data/scoring_model.json holds the rules; it has no scorer (that is Phase 6).
